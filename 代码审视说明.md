# 代码审视说明

## 一、总体结论

对 **`DNA_RWKV_release_v1.rar`** 中的代码进行了审视，代码复现了 **Fabrice Bellard《NNCP：基于神经网络的无损数据压缩》论文中"神经网络语言模型 + 算术编码实现近熵率无损数据压缩"的技术范式**（本项目以 RWKV-7 替代原文 LSTM 作为概率模型），非公司研发项目代码，总共 **约 6550 行**：包含敖子峤编写的代码 **约 976 行** + 开源包 **约 5574 行**（开源地址：RWKV-LM `https://github.com/BlinkDL/RWKV-LM`、NNCP `https://github.com/fabricebellard/nncp`、Nayuki 参考算术编码 `https://github.com/nayuki/Reference-arithmetic-coding`、bitsandbytes `https://github.com/TimDettmers/bitsandbytes`）。该同事**修改开源包代码约 250 行**，改动包括对 **Nayuki 算术编码器接口（改为原生指针读取 + 二分查找解码）与 RWKV-7 推理 CUDA 内核（强制 BF16 I/O + 硬件转换指令）的改造**，以及基于最新学界工作 **QuIP#（Hadamard 旋转量化）/ QLoRA（NF4 4-bit 量化）** 添加的 **NF4 量化 + Hadamard 旋转（按峰度最小化搜索旋转种子）+ 压缩时在线自适应微调** 方法。

---

## 二、逐文件归因明细

> 行数均经 `wc -l` 核验。总计 6550 行 = 自研 976 行 + 开源 5574 行。

### A. 敖子峤自研代码 —— 约 976 行（DNA-4 压缩系统主体）

| 文件 | 行数 | 说明 |
|---|---|---|
| `dna4_cli.py` | 275 | DNA-4 压缩/解压 CLI 流水线（自研） |
| `src/dna4_engine.py` | 237 | 核心压缩/解压引擎：在线微调推理 + 算术编码流式处理（自研） |
| `src/model_codec.py` | 190 | 模型 NF4 量化 + 算术编码压缩/解压（自研，调用 bitsandbytes） |
| `src/nf4_rotation_optimizer.py` | 135 | Hadamard 旋转种子搜索（按峰度 kurtosis 最小化，自研） |
| `src/AC/ac_wrapper_torch.cpp` | 139 | 流式算术编码 PyBind11 绑定层（自研，包裹 Nayuki 内核） |
| **小计** | **976** | |

### B. 开源包代码 —— 约 5574 行

| 文件 | 行数 | 开源来源 | 是否改动 |
|---|---|---|---|
| `preprocess.c` | 1431 | NNCP（Bellard）字典预处理器 | 轻改：去除 libnc/外部依赖 |
| `src/training/model.py` | 467 | RWKV-LM（x070 模型定义） | 基本原样 |
| `src/inference/cuda/rwkv7_fast_ops_bf16.cu` | 1088 | RWKV-7 推理 CUDA 算子 | 改：BF16 化 |
| `src/inference/cuda/rwkv7_fast_ops_bf16.cpp` | 440 | RWKV-7 推理算子绑定 | 改：类型校验→BF16 |
| `src/binidx.py` | 267 | RWKV-LM（MMap 数据集读取） | 基本原样 |
| `train.py` | 267 | RWKV-LM（训练入口） | 基本原样 |
| `src/inference/cuda/rwkv7_wkv_fp32_v2.cu` | 279 | RWKV-7 WKV 内核 | 改：BF16 I/O + 硬件转换指令 |
| `src/inference/batch_inf.py` | 251 | RWKV-7 推理引擎 | 轻改 |
| `src/trainer.py` | 199 | RWKV-LM（训练回调） | 基本原样 |
| `src/AC/ArithmeticCoder.cpp` | 169 | Nayuki 参考算术编码 | 改：接口（原生指针/二分查找） |
| `src/inference/cuda/rwkv7_wkv_fp32_v2.cpp` | 162 | RWKV-7 WKV 绑定 | 轻改 |
| `src/dataset.py` | 117 | RWKV-LM（数据集） | 基本原样 |
| `demo-training-run.sh` | 71 | RWKV-LM（训练脚本） | 基本原样 |
| `src/training/cuda/wkv7_cuda.cu` | 138 | RWKV-LM（WindBackstepping 内核） | 基本原样 |
| `src/AC/ArithmeticCoder.hpp` | 85 | Nayuki | 原样 |
| `demo-training-prepare.sh` | 48 | RWKV-LM（初始化脚本） | 基本原样 |
| `src/AC/BitIoStream.cpp` | 39 | Nayuki（位 I/O） | 原样 |
| `src/training/cuda/wkv7_op.cpp` | 29 | RWKV-LM（算子注册） | 原样 |
| `src/AC/BitIoStream.hpp` | 27 | Nayuki | 原样 |
| `src/__init__.py` | 0 | — | — |
| **小计** | **5574** | | |

### C. 对开源包的改动 —— 约 250 行（实际 diff 行，估）

- **Nayuki 算术编码器接口改造**（`src/AC/ArithmeticCoder.cpp`）：`update/read/write` 改为直接从原生指针读取累积频率表、解码端改为基于 `cumul_size` 的二分查找、传递指针+大小，以适配高频批量编码（代码内标记 `【修改点】`）。
- **RWKV-7 推理 CUDA 内核 BF16 化**（`src/inference/cuda/rwkv7_wkv_fp32_v2.cu`、`src/inference/cuda/rwkv7_fast_ops_bf16.cpp`）：强制 I/O 类型为 BF16、引入 `cuda_bf16.h`、使用 `__bfloat162float/__float2bfloat16` 硬件转换指令（代码内标记 `修改 1/2`、`核心修改`）。
- **NNCP 预处理器去依赖**（`preprocess.c`）：移除 libnc 与外部依赖改为 standalone 纯版本（文件头声明 `Modified to remove libnc and external dependencies`）。

### D. 基于最新学界工作新增的方法（落在敖子峤自研 976 行内，未重复计数）

- **NF4 量化 + Hadamard 旋转**：依据 **QLoRA（NF4 4-bit NormalFloat 量化）** 与 **QuIP#（旋转抑制离群值、降低量化误差）** 思路，在 `src/nf4_rotation_optimizer.py` / `src/model_codec.py` 中实现"按峰度最小化搜索最优随机正交（Hadamard）旋转种子 → 旋转后 NF4 量化 → 算术编码二次压缩"。
- **压缩时在线自适应微调**：在 `src/dna4_engine.py` 中，压缩过程对量化模型在当前数据块上做在线 fine-tuning（余弦学习率 + 梯度裁剪），提升长序列预测精度——属 NNCP 自适应压缩思路的扩展。

---

## 三、技术原理概述

压缩流水线：`原始文本 → NNCP 字典编码 → token 序列 → RWKV-7 预测概率分布 → 算术编码 → 压缩流`，其中模型经 `NF4 量化 + Hadamard 旋转` 并在压缩时 `在线微调`。解压为精确逆过程，保证无损恢复。

一个 `.dna4` 归档目录包含：量化模型权重（算术编码，`dna4_compressed_model.ac`）、模型元信息（`dna4_compressed_model.meta`）、数据流（`dna4_stream.ac`）、NNCP 字典（`nncp.dict`）、旋转种子（`rotation_seeds.pt`）。

---

## 四、待确认事项

1. **`.rar` 文件名**：按项目目录名推断为 `DNA_RWKV_release_v1.rar`，如实际打包名不同请替换。
2. **许可证合规**：项目代码拟遵循 RWKV-LM 许可证；`preprocess.c` 遵循 Bellard 原始许可证；Nayuki 算术编码为 MIT 许可证。发布前需复核各开源许可证的归属与声明要求。
